<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/orders.css">
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <!-- <main>
            <% if (orders.length <= 0) { %>
                <h1>Nothing there!</h1>
            <% } else { %>
                <ul class="orders">
                    <% orders.forEach(order => { %>
                        <li class="orders__item">
                            <h1>Order - # <%= order._id %> - <a href="/orders/<%= order._id %>">Invoice</a></h1>
                            <ul class="orders__products">
                                <% order.products.forEach(p => { %>
                                    <li class="orders__products-item"><%= p.product.title %> (<%= p.quantity %>)</li>
                                <% }); %>
                            </ul>
                        </li>
                    <% }); %>
                </ul>
            <% } %>
        </main> -->

        <main>
            <% if (orders.length <= 0) { %>
            <h1> No hay ordenes todavia! 📭 </h1>
            <% } else { %>
                <ul>
                    <% orders.forEach((order, i) => {  %>
                        <hr>
                        <li>
                            <h2> Orden #<%= i + 1 %>- <a href="/orders/<%= order._id %>">Ver Factura</a> </h2>
                            <ul>
                                <% order.products.forEach(product => { %>
                                    <li style="margin-bottom: 10px;"> 
                                        <span style="font-weight: bold;">
                                            <%= product.product.title %> 
                                        </span>
                                        <br />
                                        Cantidad: <span style="color: gray;"><%= product.quantity %></span>
                                        <br /> 
                                        Precio: <span style="color: gray;"><%= product.product.price %></span>
                                    </li>
                                <% }); %>
                                <!-- <span>
                                    <br />
                                    <h2 style="display: inline;">Total de Orden: <span style="color:#DD4A48; font-weight: bold;">$<%= orderTotal %></span></h2>
                                </span> -->
                            </ul>
                        </li>
                    <% }); %>
                </ul>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>

        
